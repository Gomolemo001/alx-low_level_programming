#include "main.h"

/**
 * print_most_numbers - prints numbers except 2 and 4
 *
 * Return: nothing
 */

void print_most_numbers(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		if (x !=2 && x !=4)
		putchar(x + '0');
	}
	_putchar('\n');
}
